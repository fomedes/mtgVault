/**
 * Placeholder until Phase 14 (Friends & Social) fills this in (task P14-08).
 * Kept as its own component so wiring real data later is a one-file change.
 */
export function FriendsActivity() {
  return (
    <section className="bg-card rounded-lg border p-4">
      <h2 className="mb-3 text-base font-semibold">Friends</h2>
      <p className="text-muted-foreground text-sm">
        Friends are coming soon — you&rsquo;ll be able to add friends by code,
        see who&rsquo;s online, and invite them to drafts.
      </p>
    </section>
  );
}
